import { ExpotencialPipe } from './expotencial.pipe';

describe('ExpotencialPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpotencialPipe();
    expect(pipe).toBeTruthy();
  });
});
